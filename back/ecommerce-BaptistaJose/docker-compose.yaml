
services:
  nestapp:
    build: .
    ports:
      - '8080:8080'
    env_file:
      - .env.development
    depends_on:
      - postgresdb

  postgresdb:
    image: postgres
    env_file:
      - .env.development
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: {}
